#include "main.h"
#include <string.h>
/**
 * rev_string - task4
 * @s: char
 * Return: always 0
 */
void rev_string(char *s)
{
	int l, a;

	scanf("%c", s);
	l = strlen(s);
	for (a = l - 1; a >= 0; a--)
	{}
}
